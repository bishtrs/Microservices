#Server port
server:
  port: 8888
 
#Git repo location
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: C:\\Users\\bisht\\Git\\config-repo
          
          
 
